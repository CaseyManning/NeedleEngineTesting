import { NodeIO, Transform, ILogger } from '@gltf-transform/core';
/** Helper class for managing a CLI command session. */
export declare class Session {
    private _io;
    private _logger;
    private _input;
    private _output;
    private _outputFormat;
    constructor(_io: NodeIO, _logger: ILogger, _input: string, _output: string);
    static create(io: NodeIO, logger: unknown, input: unknown, output: unknown): Session;
    transform(...transforms: Transform[]): Promise<void>;
}
